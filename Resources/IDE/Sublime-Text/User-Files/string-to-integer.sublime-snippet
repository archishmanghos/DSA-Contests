<snippet>
  <content><![CDATA[
int64_t stringToInteger (string s) {
	int64_t answer = 0, j = 1;
	while (s.size()) {
		answer += (s.back() - '0') * j;
		j *= 10;
		s.pop_back();
	}

	return answer;
}
]]></content>
  <tabTrigger>strToInt</tabTrigger>
  <description>string to integer</description>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <!-- <scope >source.python</scope > -->
</snippet>