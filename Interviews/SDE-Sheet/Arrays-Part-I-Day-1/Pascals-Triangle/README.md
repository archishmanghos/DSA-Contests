Most Optimal: TC = O(N x N), SC = O(1)

don't use any extra vector, just declare the original
vector with a reasonable size and allot future
iterations with the size of i + 1, and utilise vector's
properties